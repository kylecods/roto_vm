typedef struct{
  int instruction;
  int ival;
  char *sval;
}Token_type;

typedef enum{
  PSH,
  ADD,
  POP,
  SUB,HLT,MUL,DIV,MOV,PRT,PREG

}InstructionSet;

void start_lexer(char *file);
void next_token(void);
