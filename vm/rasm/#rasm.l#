%{
#include <stdio.h>

%}
comment (##)[^\n]*
%%
[a-zA-Z0-9]+   {;}
\"[^"\n]*["\n] {prints(yytext);}
[ \n\t]        {;}
{comment}      {;}
%%

int yywrap(){return 1;}

char *prints(char * text){
   return text;
}

